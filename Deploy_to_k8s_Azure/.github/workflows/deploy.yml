name: Build and Deploy to AKS

on:
  push:
    branches: ["main"]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Login to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Login to ACR
      run: az acr login --name ${{ secrets.ACR_NAME }}

    - name: Build & Push Image
      run: |
        IMAGE=${{ secrets.ACR_NAME }}.azurecr.io/demoapp:${{ github.sha }}
        docker build -t $IMAGE .
        docker push $IMAGE
        echo "IMAGE=$IMAGE" >> $GITHUB_ENV

    - name: Get AKS credentials
      run: |
        az aks get-credentials \
        --resource-group ${{ secrets.AKS_RESOURCE_GROUP }} \
        --name ${{ secrets.AKS_CLUSTER_NAME }}

    - name: Deploy to AKS
      run: |
        kubectl apply -f az_deployment.yaml
        kubectl apply -f az_service.yaml
        kubectl set image deployment/demo-deployment demo=$IMAGE
        kubectl rollout status deployment/demo-deployment
